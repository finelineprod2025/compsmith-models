{
  "_comment": "CompSmith AE script error knowledge base. Categories are injected selectively based on the user request keywords. Add new entries after reproducing and fixing the error in AE.",
  "_updated": "2026-06",
  "_version": 24,
  "_categories": [
    "colors",
    "shape_layers",
    "text_layers",
    "transform",
    "effects",
    "null_guards",
    "output_format",
    "general"
  ],
  "colors": {
    "_keywords": [
      "color",
      "solid",
      "background",
      "fill",
      "tint",
      "teal",
      "gradient"
    ],
    "_description": "Color array format rules for AE scripting",
    "rules": [
      {
        "id": "addSolid-rgb-only",
        "title": "addSolid() takes [R,G,B] only Ã",
        "wrong": "comp.layers.addSolid([0.1, 0.4, 0.45, 1], ...)",
        "right": "comp.layers.addSolid([0.1, 0.4, 0.45], ...)",
        "error": "After Effects error: Color Array does not have 3 values.",
        "note": "AE color arrays for addSolid are always 3-element RGB (0Ã",
        "source": "session:01.md-2026-06-06"
      },
      {
        "id": "fill-color-rgb-only",
        "title": "Fill color setValue() takes [R,G,B] Ã",
        "wrong": "fill.property('ADBE Vector Fill Color').setValue([1, 1, 0.8, 1])",
        "right": "fill.property('ADBE Vector Fill Color').setValue([1, 1, 0.8])",
        "error": "Possible silent wrong color or type error depending on AE version.",
        "note": "Shape layer fill color properties also expect 3-element RGB arrays.",
        "source": "observed-pattern"
      }
    ]
  },
  "shape_layers": {
    "_keywords": [
      "shape",
      "rect",
      "ellipse",
      "star",
      "polygon",
      "repeater",
      "trim",
      "stroke",
      "fill",
      "vector",
      "group",
      "shape layer",
      "repeat shape",
      "duplicate shape",
      "vector shape",
      "repeater position",
      "repeater transform",
      "repeater offset",
      "shape repeater",
      "hexagon grid",
      "tile pattern",
      "dunes",
      "dune",
      "mountains",
      "hills",
      "parallax",
      "layered",
      "depth",
      "background",
      "foreground",
      "covers",
      "overlap",
      "stacking",
      "z-order",
      "behind",
      "in front",
      "waves",
      "clouds",
      "scenery"
    ],
    "_description": "Shape layer creation and property access rules",
    "rules": [
      {
        "id": "repeater-pos-not-position",
        "title": "Repeater transform property is 'ADBE Vector Repeater Pos' not 'ADBE Vector Repeater Position'",
        "wrong": "rep.property('ADBE Vector Repeater Transform').property('ADBE Vector Repeater Position')",
        "right": "var rt=rep.property('ADBE Vector Repeater Transform'); var rp=rt ? rt.property('ADBE Vector Repeater Pos') : null; if(rp) rp.setValue([50,0]);",
        "error": "Returns null silently or throws Ã",
        "note": "The correct internal name is 'Pos', not 'Position'. Always null-guard the result.",
        "source": "system-prompt-observed"
      },
      {
        "id": "shape-root-null-guard",
        "title": "Always null-guard ADBE Root Vectors Group before use",
        "wrong": "var root=sl.property('ADBE Root Vectors Group'); root.addProperty(...);",
        "right": "var root=sl.property('ADBE Root Vectors Group'); if(!root) return JSON.stringify({success:false,message:'Shape root group not found.'});",
        "error": "TypeError if root is null on certain layer types.",
        "note": "Root vectors group can be null if the layer is not a shape layer.",
        "source": "system-prompt-observed"
      },
      {
        "id": "shape-group-null-guard",
        "title": "Null-guard addProperty() results for shape groups",
        "wrong": "var grp=root.addProperty('ADBE Vector Group'); grp.name='Rect 1';",
        "right": "var grp=root.addProperty('ADBE Vector Group'); if(!grp) return JSON.stringify({success:false,message:'Could not create shape group.'}); grp.name='Rect 1';",
        "error": "TypeError: Cannot set property 'name' of null",
        "note": "addProperty() can return null Ã",
        "source": "system-prompt-observed"
      },
      {
        "id": "polystar-outer-roundness-matchname",
        "title": "Use 'Outer Roundness' match name for Polystar roundness, not 'Roundness'",
        "wrong": "poly.property(\"ADBE Vector Star Roundness\")",
        "right": "poly.property(\"ADBE Vector Star Outer Roundness\")",
        "error": "Could not access Polystar Roundness property.",
        "note": "The Polystar group exposes 'ADBE Vector Star Outer Roundness' (and 'ADBE Vector Star Inner Roundness' for stars); there is no plain 'ADBE Vector Star Roundness' match name, so property() returns null. Always verify Polystar match names: Type, Points, Position, Rotation, Inner/Outer Radius, Inner/Outer Roundness.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "repeater-property-path",
        "title": "Access Repeater sub-properties via the Repeater operator group, not the Transform group",
        "wrong": "var repPos = shapeLayer.property(\"ADBE Root Vectors Group\").property(\"ADBE Vector Transform Group\").property(\"Repeater A Position\");",
        "right": "var contents = shapeLayer.property(\"ADBE Root Vectors Group\");\nvar repeater = contents.addProperty(\"ADBE Vector Filter - Repeater\");\nvar repTransform = repeater.property(\"ADBE Vector Repeater Transform\");\nvar repPos = repTransform.property(\"ADBE Vector Repeater Position\");",
        "error": "Could not access Repeater A Position.",
        "note": "Repeater Position lives under the Repeater operator's own transform group (\"ADBE Vector Repeater Transform\" Ã",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "repeater-transform-position-matchname",
        "title": "Use 'ADBE Vector Repeater Position' (not 'Pos') for a Repeater's Transform position",
        "wrong": "var repTransform = rep.property(\"ADBE Vector Repeater Transform\");\nvar repPos = repTransform.property(\"ADBE Vector Repeater Pos\"); // returns null\nrepPos.setValue([w, 0]);",
        "right": "var repTransform = rep.property(\"ADBE Vector Repeater Transform\");\nvar repPos = repTransform.property(\"ADBE Vector Repeater Position\");\nrepPos.setValue([w, 0]);",
        "error": "Could not access Repeater A Position.",
        "note": "The Repeater's Transform position match name is 'ADBE Vector Repeater Position', not 'ADBE Vector Repeater Pos'; the wrong name returns null and fails the guard. Other repeater sub-properties: 'ADBE Vector Repeater Copies', 'ADBE Vector Repeater Offset', 'ADBE Vector Repeater Anchor Point', 'ADBE Vector Repeater Rotation', 'ADBE Vector Repeater Scale'.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "shape-group-stacking-order",
        "title": "First addProperty group in a shape layer renders in FRONT — add depth layers foreground-first",
        "wrong": "for(var i=0;i<duneSizes.length;i++){ var g=root.addProperty(\"ADBE Vector Group\"); /* biggest/background group added first ends up in FRONT and covers everything */ }",
        "right": "for(var i=duneSizes.length-1;i>=0;i--){ var g=root.addProperty(\"ADBE Vector Group\"); /* foreground added first; the large background group is added last so it stacks BEHIND */ }",
        "error": "Larger/background shape covers everything else — depth order inside the shape layer is inverted",
        "note": "Inside a single shape layer's Contents, the FIRST group added with addProperty() renders frontmost and every later addProperty() stacks BEHIND it. When depth layers are described background-to-foreground (dunes, mountains, parallax hills, layered waves, clouds), iterate the array in REVERSE so the foreground is added first and the big background shape ends up behind everything. Creation order is the inverse of visual stacking — do not assume add order matches back-to-front.",
        "source": "user-added:2026-06-13"
      }
    ]
  },
  "text_layers": {
    "_keywords": [
      "text",
      "font",
      "paragraph",
      "typeface",
      "caption",
      "title",
      "subtitle",
      "label"
    ],
    "_description": "Text layer property access rules",
    "rules": [
      {
        "id": "text-props-null-guard",
        "title": "Null-guard ADBE Text Properties and ADBE Text Document before access",
        "wrong": "var srcText=tl.property('ADBE Text Properties').property('ADBE Text Document');",
        "right": "var txtProps=tl.property('ADBE Text Properties'); if(!txtProps) return JSON.stringify({success:false,message:'Text properties not found.'}); var srcText=txtProps.property('ADBE Text Document'); if(!srcText) return JSON.stringify({success:false,message:'Source text not found.'});",
        "error": "TypeError on non-text layers or if property chain returns null.",
        "note": "Chain each property access separately with a null check.",
        "source": "system-prompt-observed"
      }
    ]
  },
  "transform": {
    "_keywords": [
      "position",
      "scale",
      "rotation",
      "opacity",
      "anchor",
      "transform",
      "move",
      "animate",
      "keyframe",
      "setTemporalEaseAtKey",
      "KeyframeEase",
      "Scale",
      "Position",
      "ease array",
      "Value array does not have 3 elements",
      "parameter 2",
      "transform easing",
      "keyframes"
    ],
    "_description": "Transform group property access rules",
    "rules": [
      {
        "id": "transform-group-null-guard",
        "title": "Null-guard ADBE Transform Group before accessing sub-properties",
        "wrong": "layer.property('ADBE Transform Group').property('ADBE Position').setValue([960,540]);",
        "right": "var tr=layer.property('ADBE Transform Group'); if(!tr) return JSON.stringify({success:false,message:'Transform group not found.'}); var pos=tr.property('ADBE Position'); if(pos) pos.setValue([960,540]);",
        "error": "TypeError if transform group is null (e.g. on certain effect layers).",
        "note": "Never chain .property() calls without null checks between them.",
        "source": "system-prompt-observed"
      },
      {
        "id": "temporal-ease-array-dimensions",
        "title": "Match setTemporalEaseAtKey ease array length to AE property value dimensions",
        "wrong": "For Scale or Position, guessing the ease array length from the visible UI dimensions, e.g. sc.setTemporalEaseAtKey(1, [eIn, eIn], [eOut, eOut]); or using [eIn] for all animated properties.",
        "right": "Build the ease arrays from the animated property's actual value array length. Example: function easeArr(prop,e){var v=prop.value;var n=(v instanceof Array)?v.length:1;var a=[];for(var i=0;i<n;i++)a.push(e);return a;} sc.setTemporalEaseAtKey(1, easeArr(sc,eIn), easeArr(sc,eOut)); For AE Scale this is usually 3 values [x,y,z], even on a 2D layer.",
        "error": "Unable to call \"setTemporalEaseAtKey\" because of parameter 2. Value array does not have 3 elements.",
        "note": "After Effects expects one KeyframeEase object per property dimension. Do not assume Scale is 2D; Transform Scale commonly has 3 components. For scalar properties like Opacity or Rotation, use one ease object. For arrays, use prop.value.length.",
        "source": "user-added:2026-06-13"
      }
    ]
  },
  "effects": {
    "_keywords": [
      "effect",
      "blur",
      "glow",
      "color correction",
      "particle",
      "cc ",
      "plugin",
      "apply effect",
      "gradient ramp",
      "gradient",
      "ramp effect",
      "color fill",
      "addproperty effect",
      "ramp",
      "sky",
      "background color",
      "sunset"
    ],
    "_description": "Effect property access rules",
    "rules": [
      {
        "id": "effect-parade-null-guard",
        "title": "Null-guard ADBE Effect Parade and individual effects before access",
        "wrong": "var pw=layer.property('ADBE Effect Parade').property('CC Particle World');",
        "right": "var fx=layer.property('ADBE Effect Parade'); var pw=fx ? fx.property('CC Particle World') : null; if(!pw) return;",
        "error": "TypeError if effect is not applied or plugin not installed.",
        "note": "Effects can be missing due to plugins not installed or wrong layer type. Never assume an effect exists.",
        "source": "system-prompt-observed"
      },
      {
        "id": "canAddProperty-check",
        "title": "Check canAddProperty() before adding effects that require plugins",
        "wrong": "fx.addProperty('CC Particle Systems II');",
        "right": "if(fx.canAddProperty && !fx.canAddProperty('CC Particle Systems II')) return JSON.stringify({success:false,message:'CC Particle Systems II is not installed.'}); var particleEffect=fx.addProperty('CC Particle Systems II');",
        "error": "Silent failure or crash if plugin not installed.",
        "note": "Always gate plugin-dependent effects with canAddProperty().",
        "source": "session:02.md-2026-06-06"
      },
      {
        "id": "gradient-ramp-matchname",
        "title": "Use the correct match name when adding the Gradient Ramp effect",
        "wrong": "layer.property(\"ADBE Effect Parade\").addProperty(\"ADBE Gradient Ramp\");",
        "right": "layer.property(\"ADBE Effect Parade\").addProperty(\"ADBE Ramp\");",
        "error": "After Effects error: Can not add a property with name Ã",
        "note": "The Gradient Ramp effect's internal match name is \"ADBE Ramp\", not \"ADBE Gradient Ramp\"; addProperty must receive the exact match name or it throws. Always verify match names against the AE match name reference rather than guessing from the UI display name.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "gradient-ramp-matchname-2",
        "title": "Use the correct match name when adding the Gradient Ramp effect",
        "wrong": "layer.property(\"ADBE Effect Parade\").addProperty(\"ADBE Gradient Ramp\");",
        "right": "layer.property(\"ADBE Effect Parade\").addProperty(\"ADBE Ramp\");",
        "error": "After Effects error: Can not add a property with name Ã",
        "note": "The Gradient Ramp effect's internal match name is \"ADBE Ramp\", not \"ADBE Gradient Ramp\"; addProperty must receive the exact match name or it throws. Always verify match names against the AE match name reference rather than guessing from the UI display name.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "gradient-ramp-match-name",
        "title": "Use the correct match name 'ADBE Ramp' for the Gradient Ramp effect",
        "wrong": "var ramp = fx.addProperty(\"ADBE Gradient Ramp\");",
        "right": "var ramp = fx.addProperty(\"ADBE Ramp\");",
        "error": "After Effects error: Can not add a property with name “ADBE Gradient Ramp” to this PropertyGroup.",
        "note": "The internal match name for the Gradient Ramp effect is 'ADBE Ramp', not 'ADBE Gradient Ramp'. Adding effects by addProperty requires the exact match name; the display name does not work. The same applies to its sub-properties (e.g. 'ADBE Ramp-0001' for Start of Ramp), so prefer the display names like 'Start of Ramp'/'Start Color' or the verified match names.",
        "source": "admin-approved:2026-06-13"
      }
    ]
  },
  "null_guards": {
    "_keywords": [
      "property",
      "layer",
      "comp",
      "item",
      "group",
      "contents",
      "create comp",
      "no comp",
      "active composition",
      "active comp",
      "activeitem",
      "current composition",
      "comp first",
      "current comp",
      "open a comp",
      "openinviewer",
      "no active composition"
    ],
    "_description": "General null-guard patterns for AE property access",
    "rules": [
      {
        "id": "no-try-catch-for-properties",
        "title": "Use null-guards not try/catch for property access",
        "wrong": "var prop=null; try{prop=layer.property('Effects').property('Blur').property('Blurriness');}catch(e){}",
        "right": "var fx=layer.property('ADBE Effect Parade'); var blur=fx ? fx.property('Blur') : null; var prop=blur ? blur.property('Blurriness') : null; if(prop) prop.setValue(20);",
        "error": "try/catch swallows useful error info and masks real bugs.",
        "note": "AE properties return null on incompatible types Ã",
        "source": "system-prompt-observed"
      },
      {
        "id": "create-comp-when-none-active",
        "title": "When no comp is active, ACTIVATE an existing comp — only create one if the project has none",
        "wrong": "var comp=app.project.activeItem; if(!(comp instanceof CompItem)) return JSON.stringify({success:false,message:\"No active composition. Please open a comp and try again.\"});",
        "right": "var comp=app.project.activeItem; if(!(comp instanceof CompItem)){ for(var i=1;i<=app.project.numItems;i++){ if(app.project.item(i) instanceof CompItem){ comp=app.project.item(i); break; } } if(comp instanceof CompItem){ comp.openInViewer(); } else { comp=app.project.items.addComp(\"Comp 1\",1920,1080,1,10,24); comp.openInViewer(); } }",
        "error": "No active composition. Please open a comp and try again.",
        "note": "Do not bail and do not immediately create a new empty comp when app.project.activeItem is not a CompItem. First scan app.project.items (1-based: app.project.item(i)) for an existing CompItem and make it active with comp.openInViewer() — this targets the user's existing work instead of dumping content into a brand-new blank comp. Only call app.project.items.addComp(...) as a last resort when the project contains no comps at all.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "guard-active-comp",
        "title": "Validate app.project.activeItem is a CompItem before using it",
        "wrong": "var comp = app.project.activeItem;\ncomp.layers.addText(\"Hello\");",
        "right": "var comp = app.project.activeItem;\nif (!(comp && comp instanceof CompItem)) {\n  return JSON.stringify({success:false, message:\"Open a comp first.\"});\n}\ncomp.layers.addText(\"Hello\");",
        "error": "Open a comp first.",
        "note": "app.project.activeItem is null when no comp is open and can be a FolderItem/FootageItem when a non-comp item is selected in the Project panel; always check `instanceof CompItem` before accessing comp-specific members like .layers.",
        "source": "admin-approved:2026-06-08"
      }
    ]
  },
  "output_format": {
    "_keywords": [
      "*",
      "json result",
      "return value",
      "script output",
      "stringify result",
      "evalscript",
      "no result",
      "stringify",
      "lower thirds",
      "lower-third",
      "title card",
      "name and title",
      "broadcast graphics",
      "uhd comp"
    ],
    "_description": "Rules about how the AI must format its code output",
    "_always_inject": true,
    "rules": [
      {
        "id": "no-markdown-in-code",
        "title": "Never use markdown code fences inside the generated script",
        "wrong": "/* Star Settings */```json\n  var starBirthRate = 0.5;",
        "right": "/* Star Settings */\n  var starBirthRate = 0.5;",
        "error": "Markdown backticks inside the code string break JSON.parse() on the response envelope, causing the raw JSON to be displayed instead of the rendered script.",
        "note": "The code value in the response JSON must be pure JavaScript. No backticks, no markdown fences, no language tags.",
        "source": "session:02.md-2026-06-06"
      },
      {
        "id": "complete-iife-return-json",
        "title": "Always close the IIFE and return a final JSON string as the last evaluated expression",
        "wrong": "(function(){\n  var n=0;\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.",
        "right": "(function(){\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\"});\n    // ...work...\n    return JSON.stringify({success:true});\n  }catch(e){\n    return JSON.stringify({success:false,message:String(e)});\n  }\n})();",
        "error": "Script returned no result. Make sure After Effects is open.",
        "note": "The script was truncated mid-string so the IIFE was never closed or invoked, leaving no final evaluated value for the host to read. In ES3 there is no last-statement implicit return, so every code path must explicitly return a complete, properly quoted JSON string and the wrapper must end with `})();`.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "no-unterminated-string-literal-02",
        "title": "Never break a JS string literal across lines with a backslash-newline; close strings and return a complete result",
        "wrong": "return JSON.stringify({success:false,message:\"No project open.\\",
        "right": "return JSON.stringify({success:false, message:\"No project open.\"});",
        "error": "Script returned no result. Make sure After Effects is open.",
        "note": "In ES3 a string literal must be terminated on the same logical line; a trailing backslash before a newline (or a truncated snippet) produces a syntax error so the IIFE never returns a value, which AE reports as 'Script returned no result.' Always close every string and return a fully-formed JSON.stringify result.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "iife-must-return-string-result",
        "title": "Wrap script in a self-invoking function that always returns a final JSON string",
        "wrong": "(function(){\n  var n=0;\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\n// truncated / never closed — no return value reaches host",
        "right": "(function(){\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\"});\n    // ... build lower thirds ...\n    return JSON.stringify({success:true,message:\"Lower thirds created.\"});\n  }catch(e){\n    return JSON.stringify({success:false,message:String(e)});\n  }\n})();",
        "error": "Script returned no result. Make sure After Effects is open.",
        "note": "The IIFE was never closed and no string was returned, so the host saw no result. Every code path (success, guard, and catch) must return a complete JSON.stringify(...) string, and the function must end with })(); — ES3 has no template literals, so build messages with normal string concatenation and properly escaped quotes.",
        "source": "admin-approved:2026-06-09"
      }
    ]
  },
  "general": {
    "_keywords": [],
    "_description": "Universal IIFE structure and return value rules",
    "rules": [
      {
        "id": "complete-iife-and-return-result",
        "title": "Always close the IIFE and return a serialized result string",
        "wrong": "(function(){\n  var n=0;\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\\",
        "right": "(function(){\n  var n=0;\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\"});\n    // ... work ...\n    return JSON.stringify({success:true,count:n});\n  }catch(e){\n    return JSON.stringify({success:false,message:String(e)});\n  }\n})();",
        "error": "Script returned no result. Make sure After Effects is open.",
        "note": "The script was truncated mid-string (unterminated string literal and unclosed IIFE), so AE evaluated nothing and got no return value. Ensure every generated script is syntactically complete: terminate all string literals, close all braces/parens, and end the IIFE with })(); so its last evaluated expression returns a JSON string.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "iife-must-return-string-result",
        "title": "Top-level IIFE must always return a string result so the host receives output",
        "wrong": "(function(){\n  var n=0;\n  try{\n    if(!app.project) return JSON.stringify({success:false,message:\"No project open.\"});\n    // ... main logic, but no final return on success path\n  }catch(e){}\n})();",
        "right": "var result;\n(function(){\n  try{\n    if(!app.project){ result = JSON.stringify({success:false,message:\"No project open.\"}); return; }\n    // ... main logic ...\n    result = JSON.stringify({success:true});\n  }catch(e){\n    result = JSON.stringify({success:false,message:String(e)});\n  }\n})();\nresult;",
        "error": "Script returned no result. Make sure After Effects is open.",
        "note": "ExtendScript returns the value of the last evaluated expression; a self-invoked function's internal return value is discarded by the host. Assign the JSON string to an outer variable and put that variable as the final statement so a string is always returned on every code path, including catch.",
        "source": "admin-approved:2026-06-08"
      },
      {
        "id": "no-empty-stub-scripts",
        "title": "Generate complete, runnable scripts instead of placeholder stubs",
        "wrong": "// diag",
        "right": "var comp = app.project.activeItem;\nif (comp && comp instanceof CompItem) {\n    app.beginUndoGroup(\"Operation\");\n    // ... actual logic here ...\n    app.endUndoGroup();\n} else {\n    alert(\"Please open a composition first.\");\n}",
        "error": "diag error entry",
        "note": "A script consisting only of a comment performs no work and provides no diagnostics, so it appears to fail with no meaningful output. Always emit a full, self-contained script with an undo group and a guard that the active item is a CompItem.",
        "source": "admin-approved:2026-06-08"
      }
    ],
    "_always_inject": true
  }
}